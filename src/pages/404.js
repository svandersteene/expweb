// @flow

import React, { Fragment } from "react";
import { Link } from "gatsby";

const ErrorPage = () => (
    <Fragment>
        <p>Oh no...</p>
        <Link to="/">Home</Link>
    </Fragment>
);

export default ErrorPage;
