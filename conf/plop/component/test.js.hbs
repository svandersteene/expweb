// @flow

import React from "react";
import { shallow } from "enzyme";

import {{ name }} from "./";

test.skip("Renders", () => expect(
    shallow(<{{ name }} content="Hello" />)
).toMatchSnapshot());
